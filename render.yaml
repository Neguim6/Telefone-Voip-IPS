services:
  - type: web
    name: telefone-voip-ips
    runtime: python
    buildCommand: pip install -r voip_cli/requirements.txt
    startCommand: gunicorn voip_cli.web:app --bind 0.0.0.0:$PORT --workers 2
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
